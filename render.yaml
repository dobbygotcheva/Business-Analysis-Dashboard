services:
  - type: web
    name: business-analytics-dashboard
    env: node
    buildCommand: yarn install
    startCommand: yarn start
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: PORT
        value: 8081
